# =============================================================================
# WV Wild Outdoors - Local Development Environment Configuration
# =============================================================================
# Copy this file to .env and fill in real values
# DO NOT commit .env to version control (.gitignore excludes it)
#
# IMPORTANT: Replace ALL "CHANGE_ME_*" values before starting services
# Generate secrets with: openssl rand -base64 32
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL (Foundation Database)
# -----------------------------------------------------------------------------
# Used by all services that need persistent storage
# Generate with: openssl rand -base64 16 | tr -d '/+=' | head -c 20
POSTGRES_PASSWORD=CHANGE_ME_POSTGRES_PASSWORD

# -----------------------------------------------------------------------------
# Directus (Headless CMS for Product Catalog)
# -----------------------------------------------------------------------------
# Generate KEY and SECRET with: openssl rand -base64 32
# These MUST be 32+ characters for security
DIRECTUS_KEY=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_BASE64_32
DIRECTUS_SECRET=CHANGE_ME_GENERATE_ANOTHER_WITH_OPENSSL_RAND_BASE64_32

# Admin user credentials (created on first start)
DIRECTUS_ADMIN_EMAIL=admin@localhost.dev
DIRECTUS_ADMIN_PASSWORD=CHANGE_ME_ADMIN_PASSWORD

# Directus database credentials
DIRECTUS_DB_PASSWORD=CHANGE_ME_DIRECTUS_DB_PASSWORD

# -----------------------------------------------------------------------------
# Ghost (Blog Publishing Platform)
# -----------------------------------------------------------------------------
GHOST_DB_PASSWORD=CHANGE_ME_GHOST_DB_PASSWORD

# -----------------------------------------------------------------------------
# Listmonk (Email Newsletter Management)
# -----------------------------------------------------------------------------
LISTMONK_DB_PASSWORD=CHANGE_ME_LISTMONK_DB_PASSWORD

# -----------------------------------------------------------------------------
# Mixpost (Social Media Scheduling)
# -----------------------------------------------------------------------------
# Generate Laravel app key with: echo "base64:$(openssl rand -base64 32)"
MIXPOST_APP_KEY=CHANGE_ME_GENERATE_WITH_BASE64_OPENSSL_RAND

# Mixpost database credentials
MIXPOST_DB_PASSWORD=CHANGE_ME_MIXPOST_DB_PASSWORD

# Social platform credentials (obtain from Facebook Developer Portal)
FACEBOOK_APP_ID=CHANGE_ME_FACEBOOK_APP_ID
FACEBOOK_APP_SECRET=CHANGE_ME_FACEBOOK_APP_SECRET

# -----------------------------------------------------------------------------
# Optional Feature Flags
# -----------------------------------------------------------------------------
# Set to true to load seed data on first run
ENABLE_SEED_DATA=false

# Logging verbosity (debug, info, warn, error)
LOG_LEVEL=info

# =============================================================================
# QUICK SETUP COMMANDS
# =============================================================================
# Generate all secrets at once (copy/paste each output to corresponding var):
#
#   # PostgreSQL password:
#   openssl rand -base64 16 | tr -d '/+=' | head -c 20; echo
#
#   # Directus KEY:
#   openssl rand -base64 32
#
#   # Directus SECRET:
#   openssl rand -base64 32
#
#   # Mixpost APP_KEY (include "base64:" prefix):
#   echo "base64:$(openssl rand -base64 32)"
#
#   # Database passwords (run 4x for each service):
#   openssl rand -base64 16 | tr -d '/+=' | head -c 20; echo
#
# Or use Python if openssl not available:
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# =============================================================================
