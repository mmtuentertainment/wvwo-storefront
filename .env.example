# =============================================================================
# WV Wild Outdoors - Local Development Environment Configuration
# =============================================================================
# Copy this file to .env and fill in real values
# DO NOT commit .env to version control (.gitignore excludes it)
#
# ⚠️  LOCAL DEVELOPMENT ONLY - NOT FOR PRODUCTION ⚠️
# The passwords below work out-of-the-box with the docker-compose.yml and
# init scripts. For PRODUCTION, generate strong unique passwords for ALL values.
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL (Foundation Database)
# -----------------------------------------------------------------------------
# Used by all services that need persistent storage
POSTGRES_PASSWORD=devpassword123

# -----------------------------------------------------------------------------
# Directus (Headless CMS for Product Catalog)
# -----------------------------------------------------------------------------
# Generate KEY and SECRET with: openssl rand -base64 32
# These MUST be 32+ characters for security
DIRECTUS_KEY=REPLACE_WITH_RANDOM_32_CHAR_STRING_GENERATE_WITH_OPENSSL
DIRECTUS_SECRET=REPLACE_WITH_ANOTHER_RANDOM_32_CHAR_STRING_FOR_JWT

# Admin user credentials (created on first start)
DIRECTUS_ADMIN_EMAIL=admin@localhost.dev
DIRECTUS_ADMIN_PASSWORD=admin123

# Directus database credentials
DIRECTUS_DB_PASSWORD=directuspass123

# -----------------------------------------------------------------------------
# Ghost (Blog Publishing Platform)
# -----------------------------------------------------------------------------
GHOST_DB_PASSWORD=ghostpass123

# -----------------------------------------------------------------------------
# Listmonk (Email Newsletter Management)
# -----------------------------------------------------------------------------
LISTMONK_DB_PASSWORD=listmonkpass123

# -----------------------------------------------------------------------------
# Mixpost (Social Media Scheduling)
# -----------------------------------------------------------------------------
# Generate Laravel app key with: php artisan key:generate
# Or use: echo "base64:$(openssl rand -base64 32)"
MIXPOST_APP_KEY=base64:REPLACE_WITH_LARAVEL_APP_KEY_BASE64_ENCODED

# Mixpost database credentials
MIXPOST_DB_PASSWORD=mixpostpass123

# Social platform credentials (use mock values for local dev)
FACEBOOK_APP_ID=local-dev-mock
FACEBOOK_APP_SECRET=local-dev-mock

# -----------------------------------------------------------------------------
# Optional Feature Flags
# -----------------------------------------------------------------------------
# Set to true to load seed data on first run
ENABLE_SEED_DATA=false

# Logging verbosity (debug, info, warn, error)
LOG_LEVEL=info

# =============================================================================
# QUICK SETUP COMMANDS
# =============================================================================
# Generate Directus secrets:
#   openssl rand -base64 32
#   openssl rand -base64 32
#
# Generate Mixpost app key:
#   echo "base64:$(openssl rand -base64 32)"
#
# Or use Python if openssl not available:
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# =============================================================================
