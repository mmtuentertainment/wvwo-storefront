---
import { SITE_CONTACT } from '../config/siteContact';
---

<section class="max-w-7xl mx-auto px-4 py-8 md:py-12" aria-labelledby="contact-strip-heading">
  <div class="bg-brand-cream border border-brand-mud/10 rounded-sm px-6 py-6 md:px-8 md:py-8 flex flex-col md:flex-row items-start md:items-center justify-between gap-4 shadow-sm">
    <div>
      <h2 id="contact-strip-heading" class="text-brand-brown font-display font-bold text-2xl md:text-3xl mb-2">
        Ready for your next season?
      </h2>
      <p class="text-brand-mud font-body">
        Give us a call or stop by the shop in Birch River / Little Birch, WV.
      </p>
    </div>
    <div class="flex flex-col sm:flex-row gap-3 w-full md:w-auto">
      <a
        href={SITE_CONTACT.phoneHref}
        class="inline-flex items-center justify-center px-8 py-3 bg-sign-green text-white font-bold rounded shadow hover:bg-green-800 transition-colors text-center"
      >
        Call the Shop
      </a>
      <a
        href={SITE_CONTACT.mapsUrl}
        class="inline-flex items-center justify-center px-8 py-3 border-2 border-brand-brown text-brand-brown font-bold rounded hover:bg-brand-brown hover:text-white transition-colors text-center"
      >
        Open in Maps
      </a>
    </div>
  </div>
</section>
